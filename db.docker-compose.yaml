services:
  postgres:
    container_name: gasfree-demo-postgres
    image: pgvector/pgvector:pg18
    ports:
      - "5433:5432"
    volumes:
      - gasfree-demo-postgres:/var/lib/postgresql/data/pgdata
    env_file:
      - .env.global
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
      - DEBUG=true

  redis:
    container_name: gasfree-demo-redis
    image: redis:alpine
    ports:
      - "6380:6379"
    volumes:
      - gasfree-demo-redis:/data

volumes:
  gasfree-demo-postgres:
  gasfree-demo-redis:
